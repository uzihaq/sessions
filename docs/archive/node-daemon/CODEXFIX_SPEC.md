# Fix: reattached/existing codex sessions render empty (pretty last/transcript return [])
Work ONLY in /Users/uzair/pretty-PTY-codexfix. NEVER touch the live daemon. Build clean. No commit.

BUG (live-verified, board tsk_0c96481a): Claude sessions have full structured support (`pretty last --json`, `transcript`, Remote view). But EXISTING/REATTACHED codex sessions return [] / "no messages" and show an empty Pretty view. Two root causes:

1. prettyd/src/sessions.ts — the codex watcher (watchCodexRollout, imported from codexWatcher.js) is only started for FRESHLY-CREATED codex sessions. Sessions reattached via discoverRunners() on daemon start do NOT get a watcher. FIX: in the reattach/registerRunner path (mirror how the claude JSONL watcher attaches — search where watchSessionFile / fileWatcher is set for claude, and where the codex branch decides to watch), ensure a codex session started via discovery also gets watchCodexRollout attached. Read the current code first: find where `classifyTool` / codex vs claude branching happens on both the create AND the discover paths, and make the discover path attach the codex watcher just like create does.

2. prettyd/src/codexResolver.ts — the resolver that maps a session to its ~/.codex/sessions/YYYY/MM/DD/rollout-*.jsonl only scans TODAY + YESTERDAY date dirs, so a session whose rollout is older doesn't resolve. FIX: widen the scan to cover the session's createdAt date (or scan back N days / all recent). Keep it bounded (don't scan all history every call) — resolve from the session's own start date forward, or a reasonable window (e.g. last 7 days) if createdAt isn't available.

APPROACH: read sessions.ts (create vs discover paths, the codex branch), codexWatcher.ts, codexResolver.ts, codexResolver's date-dir logic. Make the surgical change so reattached codex sessions resolve their rollout AND get a live watcher, so `pretty last <id> --json` and `pretty transcript <id>` return real data and the Remote view populates.

ACCEPTANCE (CODEXFIX_NOTES.md): explain the exact before/after for both fixes with file:line. If you can, prove against a real reattached codex session id from the live state dir (READ-ONLY: `pretty last <existing-codex-id> --json` using THIS worktree's built daemon is NOT possible without running a daemon — instead, unit-test the resolver: point codexResolver at ~/.codex/sessions and assert it resolves a known older rollout by cwd+date that today+yesterday-only would miss). `cd prettyd && npm run build` + `node -c bin/pretty.cjs` clean. Do NOT commit, do NOT touch the live daemon.
