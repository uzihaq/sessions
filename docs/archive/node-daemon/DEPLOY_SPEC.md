# `pretty deploy` — the canonical safe deploy (kills the recurring dep-crash class)
Work ONLY in /Users/uzair/pretty-PTY-deploy. Files: prettyd/bin/pretty.cjs (new cmdDeploy + help + dispatch) and scripts/install.sh. Gate: node -c bin/pretty.cjs. No commit. NEVER run the deploy against the live daemon — logic only; you may run `pretty deploy --dry-run` against the repo.
Background: TWICE the live daemon crash-looped because a merge changed prettyd/package.json but node_modules wasn't installed before the restart (web-push, dompurify). The deploy contract must make that impossible.
Build `pretty deploy [--repo <dir>] [--no-pull] [--dry-run]`:
1. Resolve repo dir (default: walk up from the bin's realpath to the git root; --repo overrides). Refuse if working tree has conflicts.
2. Steps, each printed + abortable: git pull --ff-only (skip with --no-pull) → npm install in prettyd/ AND frontend/ (ALWAYS — cheap when no-op) → npm run build in prettyd/ → vite build in frontend/ → PRE-FLIGHT: node -e "require or import dist/server.js resolvability" (catch missing deps BEFORE restarting; simplest: node --input-type=module -e "await import('<repo>/prettyd/dist/server.js')" with a 5s timeout and PRETTYD_SMOKE=1 env the server can honor to exit immediately after imports — add a tiny early-exit in src/server.ts guarded by process.env.PRETTYD_SMOKE) → record runner baseline (pgrep -f dist/runner.js | wc -l) → launchctl kickstart -k gui/$UID/tech.pretty-pty.daemon → poll /api/health (read host/port from the health listen field or PRETTYD_HOST) up to 30s → verify runner count >= baseline-1 → print PASS/FAIL summary. --dry-run prints the plan without executing pull/install/build/kickstart but DOES run the resolvability preflight.
3. Update scripts/install.sh to delegate to `pretty deploy` semantics (or share the same steps).
Write DEPLOY_NOTES.md: show --dry-run output + node -c. No commit.
