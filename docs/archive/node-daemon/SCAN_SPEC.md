# Codex resolver: full-scan fallback (kill the no-cwd-match dead end)
Work ONLY in /Users/uzair/pretty-PTY-scan. File: prettyd/src/codexResolver.ts (+ its test script). Gate: npm run build clean + the existing scripts/test-codex-resolver.mjs still passes. No commit. Never touch the live daemon.

LIVE BUG (proven): 3 real reattached fresh codex sessions resolve to reason 'no-cwd-match' — their rollout files are in date dirs OUTSIDE the scanned {today, yesterday, createdAt} window (e.g. respawned runners whose conversations started on other dates). resolveCodexRolloutPath (codexResolver.ts:209-255) gives up.

FIX: when the bounded 3-day scan yields no cwd match, FALL BACK to a full scan: walk ALL of ~/.codex/sessions (listRolloutsRecursive already exists, line ~126) and readSessionMeta (first-line only, already bounded) on each rollout, matching meta.cwd === opts.cwd. Selection among matches: prefer the one whose meta.timestampMs is closest to opts.createdAt; drop the strict `timestampMs < createdAt → skip` filter in the fallback (respawned runners legitimately attach to older conversations) but prefer candidates <= a few minutes after createdAt over much-later ones. Return a new reason 'fresh-match-fullscan' with ambiguousCount as today. PERF: full scan runs ONLY on the fallback path (attach-time, once per session, not in any loop); cache nothing; keep first-line reads (FIRST_LINE_BYTES). If still nothing: keep existing 'no-cwd-match'.

PROVE IT (SCAN_NOTES.md): extend/add a node test that runs the resolver against the THREE real live failing sessions' metadata (read-only from ~/.local/state/pretty-PTY/runners: ids starting 0cae0b90, 0ecc9dcd, 0fa852c4 — read their .json for cwd/args/createdAt) and show each now resolves to a real rollout path (or, if a session GENUINELY has no rollout with that cwd anywhere, print that finding explicitly — do not fake success). npm run build clean, existing resolver test still green. No commit.
