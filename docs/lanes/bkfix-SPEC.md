# Lane: BKFIX — deflake internal/backup under full-suite load
Work ONLY in /Users/uzair/pretty-PTY-bkfix. internal/backup tests pass isolated but FAIL rarely under full-suite parallel load (`go test ./... -count=1`). Reproduce under load (loop full suite, or run backup with a concurrent `go test ./internal/mirror -count=10` hammering CPU). NAME the failing test (candidates: TestPushRawTranscriptManifestAndIncrementalSkip — mtime/size incremental cache; TestPeriodicServiceRunsOnlyWhenEnabled — timer/goroutine timing). Diagnose PRODUCT race vs TEST timing (run go test ./internal/backup -race -count=20). Product race → fix (likely the incremental size+mtime cache racing, or the periodic timer). Test timing → synchronize on observable state / controllable clock. NO weakened assertions, NO sleeps-as-fix, NO skip. Prove: 10 consecutive full-suite -count=1 green + backup -race -count=20 green. docs/lanes/bkfix-NOTES.md. No commit.
