# Lane: WAIT — wake-on-condition (the dogfooded killer feature)
Work ONLY in /Users/uzair/pretty-PTY-wait. FILE OWNERSHIP: internal/waitcond/ (new), cmd/pretty/waituntil.go (new; extend existing wait command ONLY via one dispatch hook in the wait entrypoint), internal/api (new handler file wait_handlers.go + route registrations).
DESIGN PINS (from an adopted architectural review — follow): observation only, git is truth. (1) `pretty wait <id> --until commit [--timeout] [--json]`: resolve session cwd (daemon API; fallback to runner metadata file if daemon down — the wait must survive daemon restarts), baseline = git rev-parse HEAD in that cwd (worktree-aware via git -C), fs.watch equivalent (fsnotify) on gitdir/logs/HEAD as WAKE HINT only + 5s poll fallback, satisfied when HEAD != baseline; detect history-rewrite (merge-base --is-ancestor) → flag history_rewritten, still satisfied. Output {session,cwd,baseline,commit,subject,elapsed_ms,history_rewritten}; exits 0 satisfied / 2 timeout / 1 precondition (not a git repo, no such session). (2) --until-file-contains FILE STRING (literal bytes, replace/delete/recreate-safe, capped reads). (3) --until-idle-stable DUR: idle continuously for DUR by the daemon's working flag — label the result source: codex/claude sessions = the structured classifier (authoritative-ish), raw terminal = heuristic; NEVER claim "genuinely idle" beyond evidence (field: source). (4) `--any` across multiple ids/conditions: first satisfied wins, report which (the orchestrator select — I poll-loop all day; this kills it).
GATES: build/vet/full-suite green; e2e tests in scratch git repos: commit-wait fires on real commit (subprocess), timeout exit 2, force-reset flags history_rewritten, file-contains on append+recreate, --any returns first of two. docs/lanes/wait-NOTES.md real output. No commit.
