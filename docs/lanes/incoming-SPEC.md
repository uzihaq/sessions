# Lane: RUNNER — Go runner interop-compatible with the TS daemon
Work ONLY in /Users/uzair/pretty-PTY-runner. Read prettygo/ARCHITECTURE.md — pins are law (pure Go, CGO_ENABLED=0, creack/pty). Normative source: prettyd/src/runner.ts (+ the daemon-side socket client in prettyd/src/, find it) and state-dir conventions in sessions.ts. Build in prettygo/: cmd/runner/ + internal/proto/ + internal/state/ (the parts you need). No commit.
GOAL: a Go binary that IS a pretty runner: spawns the requested cmd in a PTY (creack/pty, fixed 300x50 like config.ts), serves the unix socket with the EXACT frame protocol the TS daemon speaks, writes {id}.json metadata + .events (exact on-disk framing) + .log in the state dir, handles input/resize/kill/HELLO, survives daemon disconnects, exits cleanly when child exits (recording exit status like TS runner does).
ACCEPTANCE (the interop proof, in NOTES.md with real output): script that (1) starts the Go runner for `bash -i` with a scratch state dir /tmp/gorunner-state and a fresh uuid; (2) starts the EXISTING TS daemon against that SAME scratch dir (PRETTYD_STATE_DIR=/tmp/gorunner-state PRETTYD_PORT=8898 node prettyd/dist/server.js); (3) proves via daemon HTTP: GET /api/sessions lists the session (discovered+attached), POST input `echo INTEROP_$RANDOM` reaches the PTY, and the snapshot endpoint returns output containing that marker; (4) kill path works. NEVER touch the default state dir or any real daemon. go vet + go build clean, CGO_ENABLED=0.
