# Lane: VERDICT+STATUS — structured observability (explicit producer protocol)
Work ONLY in /Users/uzair/pretty-PTY-verdict. FILE OWNERSHIP: internal/verdict/ (new), cmd/pretty/status.go + verdict.go (new) + dispatch one-liners, api additions in NEW file verdict_handlers.go.
PINS (adopted design): pretty NEVER extracts verdicts from prose — producers EMIT them explicitly. Observation only.
1. VERDICT CHANNEL: `pretty verdict emit <session-or-lane-id> --json '{...}'` (or stdin) validating {schemaVersion:1, verdict: "pass"|"fail"|"blocked"|string, findings?: [{severity, title, detail?, file?, line?}], meta?: object} → stored append-only at <id>.verdicts.jsonl in state dir + ledger activity event (type verdict, NO content in ledger — pointer only). `pretty verdict <id> [--json]` returns the LATEST (the stale-verdict bug from the wishlist: latest-wins semantics, monotonic seq + emitted_at). Also GET/POST /api/sessions/:id/verdict.
2. `pretty status <id> --json`: one call = {id, name, kind, tool, state (working|idle|exited), exit_code?, cwd, git: {branch, head, dirty_count} (git -C cwd, null if not a repo), last_verdict?: summary, last_activity_at, created_at, age_ms}. Human output: a compact card. This kills the snap|sed|eyeball loop.
GATES: build/vet/full-suite green; tests: verdict schema validation (reject junk), latest-wins across 3 emits, status --json field table against a real scratch session incl git fields in a scratch repo. docs/lanes/verdict-NOTES.md real output. No commit.
