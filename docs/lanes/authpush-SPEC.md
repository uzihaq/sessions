# Lane: AUTHPUSH — secure-default token auth + push notifications e2e (Go)
Work ONLY in /Users/uzair/pretty-PTY-authpush. FILE OWNERSHIP: internal/api/ (auth middleware + push handlers), internal/state/config.go (auth config), cmd/pretty/ (a `pretty token` helper file token.go). Coordinate: other lanes add handler FILES + routes; you own the auth middleware + config — keep changes there.
1. TOKEN AUTH SECURE-DEFAULT (board tsk_85eadff0): confirm/finish the Go posture — loopback exempt (already built), token required for all non-loopback, `open` escape-hatch honored, and the token is auto-generated + stored 0600 (like TS config.ts getAuthToken). `pretty token` prints it (for pasting into the hosted app / another device). Verify the WS upgrade enforces the same. The DEFAULT must be secure (token on for remote) with NO manual step — a fresh install is safe out of the box. Tests: auth matrix (loopback no-token ok, remote no-token 401, remote token ok, open-file disables, XFF defeats loopback exemption).
2. PUSH E2E (board tsk_a2d8b715): verify the Go webpush path works end-to-end — VAPID keys generated+persisted, GET /api/push/vapid, POST subscribe/unsubscribe, and a notification actually SENT on the working→idle edge + on lane death (reuse the notification code). Test with a MOCK push endpoint (httptest) asserting a POST with a valid encrypted payload is made when a session goes idle. Confirm the 🟢/🟡/🔴 shapes.
GATES: build/vet/full-suite green; auth matrix test + push-send-to-mock test green. docs/lanes/authpush-NOTES.md real output. No commit. Scratch only.
