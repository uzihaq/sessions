# Lane: INTEGRATE the ledger branch into the sessions-manager architecture
Work ONLY in /Users/uzair/pretty-PTY-integ2 (branch go-integrate-ledger, from go-rewrite which now has session.Manager as the composition root + the CLI). The go-ledger BRANCH exists with internal/ledger fully tested — `git merge go-ledger` will conflict in cmd/prettyd/main.go, internal/api/server.go, internal/state/session.go. Your job is the principled resolution. No commit of unresolved state; finish with a completed merge commit? NO — resolve in working tree, do NOT commit (reviewer commits).
RESOLUTION PRINCIPLES:
1. internal/ledger is adopted WHOLE (it is green + crash-simmed; do not modify its API unless compile-forced — document any forced change).
2. session.Manager (HEAD) is the canonical composition root. Wire ledger INTO the manager: manager owns the write-ahead boundaries (RecordCreated before launch in createSession; RecordUserKill tombstone BEFORE kill) and emits observational events (runner_lost, attached, idle, activity) where the manager already detects them. If ledger's NewRegistryWithLedger approach conflicts with the manager, adapt the WIRING, not the ledger internals.
3. Kill path must compose BOTH lanes' semantics: mass-kill guard (force flag, >N sweep refusal — HEAD) AND write-ahead tombstone (go-ledger). Order: guard check → tombstone commit → kill. A guard-refused kill writes NO tombstone.
4. state/session.go: keep HEAD's recordClaudeLocked refactor; fold go-ledger's providerActivityAt extraction INTO it (activity timestamps feed ledger activity events, coalesced).
5. Also add: startup daemon_restart event, discovery→ledger reconciliation stub emitting runner_lost for ledger-known-but-absent lanes (full recover command comes later; just the events now).
GATES: CGO_ENABLED=0 build/vet green; FULL test suite -count=1 green (all existing session+ledger+api tests preserved — do not delete/weaken to pass); ADD a test proving guard+tombstone composition order. docs/lanes/integ2-NOTES.md with real output. No commit.
