# Lane: EMBED+PACKAGE — single binary with the UI inside
Work ONLY in /Users/uzair/pretty-PTY-embed. FILE OWNERSHIP (hard boundary — another lane is editing cmd/prettyd/main.go): you may create NEW files only in cmd/prettyd/ (e.g. web_embed.go), plus prettygo/scripts/, prettygo/Makefile, prettygo/internal/webassets/. You may make ONE minimal edit to main.go/server wiring ONLY if an embed hook is impossible otherwise — prefer an init()-registered fs.FS the api server consults (internal/api already serves static with a dir-fallback; add an optional embedded-FS source it prefers when the disk dir is absent, mirroring BUNDLED_WEB_DIR semantics from prettyd/src/http.ts).
SCOPE: (1) build the frontend (cd frontend && npm run build — node_modules symlinked) and go:embed the dist into the binary via internal/webassets (embed.FS behind a build tag `embedui` so plain dev builds stay fast; document). (2) Makefile/build script: `make binaries` → dist-go/pretty-darwin-arm64, pretty-linux-arm64, pretty-linux-amd64 (CGO_ENABLED=0, -trimpath, ldflags version stamp from git describe); single binary = CLI with daemon+runner as subcommands (pretty daemon / pretty runner-exec) IF cmd layout allows wrapping without touching other lanes' files — else 3 binaries per platform, document choice. (3) `pretty install` plist template pointing at the Go binary path (generate alongside; do NOT install anywhere).
GATES: make binaries succeeds; the darwin binary with embedded UI: run on scratch port/state, curl / returns the built index.html (contains <title>), /api/health 200; file(1) shows static executables; sizes reported. docs/lanes/embed-NOTES.md real output. No commit. Scratch only.
