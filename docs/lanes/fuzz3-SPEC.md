# Lane: FUZZ3 — harden the NEW structured-event parsers (codexapp + claudep)
Work ONLY in /Users/uzair/pretty-PTY-fuzz3. FILE OWNERSHIP: internal/codexapp + internal/claudep (add *_test.go fuzz/property tests; fix PRODUCT bugs found ONLY in these packages). These are this session's newest code and they PARSE UNTRUSTED EXTERNAL JSON (app-server JSON-RPC events; claude -p stream-json line events) — prime fuzz surface.
TARGETS:
- codexapp: FuzzParseServerEvent / FuzzDecodeJSONRPC — feed arbitrary/adversarial bytes to the app-server event/JSONRPC decoder (truncated frames, wrong types, huge numbers, missing ids, malformed nested item/usage, non-UTF8, duplicate/out-of-order notifications). Must never panic; malformed → error/skip, never crash the client. Also a concurrency test: the event stream channel under -race with a producer + consumer.
- claudep: FuzzParseStreamJSONLine — feed adversarial claude -p stream-json lines (partial JSON, unknown type, missing session_id, giant content arrays, malformed result/usage, interleaved). Must never panic; the turn's history stays consistent; session_id correlation robust to garbage.
A crash = product bug → fix in-package + seed corpus (testdata/fuzz/). NO weakened assertions, NO skips. If a fuzz finding is a real robustness bug in how a malformed event is handled, fix it (the daemon must never crash on a weird event from codex/claude).
GATES: CGO_ENABLED=0 build/vet green; go test ./internal/codexapp ./internal/claudep -race -count=1 green; each fuzz target -fuzztime=45s NO crash (paste output); full suite green. docs/lanes/fuzz3-NOTES.md with any bug found + seed. No commit. Scratch only.
