# Lane: CXDEFLAKE — deflake internal/codexapp under full-suite load
Work ONLY in /Users/uzair/pretty-PTY-cxdeflake. internal/codexapp passes isolated but TestResumeConversationRestoresTurnDefaults FAILS ~1/3 under full-suite parallel load (`CGO_ENABLED=0 go test ./... -count=1`). Reproduce under load (loop the full suite, or run codexapp with concurrent `go test ./internal/mirror -count=10` hammering CPU). Diagnose PRODUCT race vs TEST timing (go test ./internal/codexapp -race -count=20 REQUIRED). Product race → fix (the codexapp client uses channels/goroutines for the event stream + turn state — likely a race on turn-default state or a channel-timing assumption). Test timing → synchronize on observable state / controllable clock. NO weakened assertions, NO sleeps-as-fix, NO t.Skip.
GATES: 10 consecutive full-suite -count=1 green + codexapp -race -count=20 green (paste output) + a one-paragraph root cause. docs/lanes/cxdeflake-NOTES.md. No commit.
