# Lane: LAUNCH — the binary distribution + public launch story (roadmap v0.1, Go edition)
Work ONLY in /Users/uzair/pretty-PTY-launch. FILE OWNERSHIP: README.md, docs/ (INSTALL.md, ARCHITECTURE.md updates), prettygo/scripts/ (packaging), a Homebrew formula, site/ (landing links only — do NOT redesign the phosphor pages, just add correct install commands + a link to docs). NO prettygo Go-source edits (other lanes own those).
1. README.md rewrite for the GO binary reality: what it is + the trust contract (dumb pipe, zero LLM, no phone-home, MIT), install via `brew install uzihaq/tap/pretty` OR download the static binary (no Node, no npm, no install scripts — the whole point), quickstart (pretty install → localhost:8787), the CLI in 60s (new/ls/send/ask/wait/run/lanes/status/recover/remote/model/kill), notifications+hooks, remote (early access), the trust contract, troubleshooting (pretty doctor), development (prettygo build, lanes). Honest, <200 lines.
2. Homebrew formula (Formula/pretty.rb) pointing at a github release tarball of the darwin/linux static binaries (placeholder URL + sha256 TODO markers clearly flagged); scripts/release.sh that builds all platforms (make binaries), tars per-platform, and prints the sha256s to fill in. Document the tap-creation steps in docs/RELEASE.md.
3. site/: on index.html + setup.html add the correct `brew install` / binary-download commands and a docs link. Keep the existing design; minimal edits only. Verify no external scripts added.
GATES: README + docs render sanity (headers, code fences); release.sh --help / dry behavior; formula is valid Ruby (ruby -c Formula/pretty.rb). docs/lanes/launch-NOTES.md. No commit.
