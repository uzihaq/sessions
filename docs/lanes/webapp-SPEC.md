# Lane: WEBAPP — reach pretty by WEBSITE, not an IP
Work ONLY in /Users/uzair/pretty-PTY-webapp. FILE OWNERSHIP: frontend/ (app), site/ (hosted entry), prettygo/scripts/ (a deploy helper) — NO prettygo Go source edits.
GOAL: a person goes to a WEBSITE (pretty-pty.somewhere.site) instead of typing http://100.86.76.84:8787, and ends up driving THEIR OWN daemon. Dumb pipe preserved: the site is STATIC, talks directly to the user's daemon; we never proxy/see traffic.
BUILD:
1. HOSTED APP SHELL: make the full React app deployable as a static site to pretty-pty.somewhere.site (not just a landing/connect page — the actual pretty UI). It boots into a SERVER PICKER: remembered servers (localStorage) + "add server" (endpoint URL or the #endpoint=&token= fragment connect flow that already exists) + a localhost quick-connect. Once connected it IS pretty, running against the user's daemon over their configured endpoint (https via tailscale, or http://localhost). Reachability + token handled by the existing servers.ts + connect bootstrap — wire them into a clean first-run.
2. FIRST-RUN UX: if no server configured, a friendly connect screen (how to install pretty, run `pretty remote enable`, scan/enter the endpoint) — reuse site/connect.html teachings. If a server is configured + reachable, go straight to the session list.
3. LOCAL NAME too: the Go daemon already serves its own embedded UI; ensure that same build works when served from the daemon (localhost:8787) AND when hosted static (pretty-pty.somewhere.site pointed at a remote daemon). One build, both modes (base-path safe, no hardcoded origin — API calls go to the selected server, not window.origin, when a server is selected).
4. DEPLOY: a script prettygo/scripts/deploy-site.sh that builds the frontend and stages it for `somewhere deploy` to the pretty-pty project (document the somewhere deploy command; do NOT run a real deploy — dry stage + instructions).
GATES: npx tsc --noEmit + npx vite build clean; a headless-browser (puppeteer in frontend/node_modules) smoke: load the built app with NO server configured → connect screen renders; inject a server config for the local soak daemon (http://127.0.0.1:8787) → session list renders. Screenshot both. docs/lanes/webapp-NOTES.md. No commit. Never touch the mini.
