# Lane: LEDGER — the lane ledger, native (board tsk_64772bd2 design, adopted verbatim)
Work ONLY in /Users/uzair/pretty-PTY-ledger. Build prettygo/internal/ledger/ + wire-in points. Pins law. modernc.org/sqlite (pure Go — CGO_ENABLED=0 must hold). No commit; scratch dirs only.
DESIGN (from the codex-designed spec — implement faithfully): single sqlite at ~/Library/Application Support/pretty-PTY/ledger/lanes.sqlite3 (0600/0700; path overridable via env PRETTY_LEDGER_PATH for tests/scratch) — OUTSIDE the runner state dir. Append-only lane_events(seq PK, event_id UNIQUE, lane_id, type, at_ms, actor, schema_version, payload_json). WAL, synchronous=FULL, busy timeout, txns. Event types: created/launch_started/runner_ready/provider_bound/attached/activity(coalesced)/idle/renamed/user_kill_requested/runner_exited/runner_lost/reaped/reopened/daemon_restart. created payload: name/tool/cwd/safe argv (resume recipe)/lane uuid/provider uuid. NO prompts/terminal bytes/secrets.
WRITE-AHEAD BOUNDARIES as a typed API: RecordCreated (commit BEFORE launch; error → caller must not launch), RecordUserKill (tombstone BEFORE kill; error → abort kill) — and observational writers (RunnerLost, Reaped...) as a SEPARATE interface that CANNOT write tombstones (compile-level separation + test).
FOLD + CLASSIFY: deterministic reduce → live-managed/closed/unexpectedly-lost/external + anomalies (closed-but-running, never-became-ready, resume-source-missing, provider-unbound). Recovery plan builder: for unexpectedly-lost lanes produce the create-with-resume recipes, recency-ranked (last activity = human-input/provider-event time only).
WIRE: hooks in internal/session (if the sessions lane's code is present, integrate; else define the interface it will call). GATES: build/vet/test green; tests: write-ahead ordering (created committed before "launch"), tombstone-wins-forever, classification table across all 4+anomaly cases, crash-sim (kill -9 a writer process mid-txn via a helper binary, DB stays valid + WAL recovers). docs/lanes/ledger-NOTES.md real output. No commit.
