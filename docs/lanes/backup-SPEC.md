# Lane: BACKUP — opt-in session backup to the user's somewhere account (roadmap value ladder)
Work ONLY in /Users/uzair/pretty-PTY-backup. FILE OWNERSHIP: internal/backup/ (new), cmd/pretty/backup.go (new) + one dispatch line in commands.go, api handler in NEW file backup_handlers.go + route registration. Design pins: pretty NEVER sees/relays data through OUR servers — the daemon pushes the user's OWN conversation files DIRECTLY to the user's OWN somewhere account. Opt-in only.
1. `pretty backup enable --project <somewhere-project>` stores config (somewhere token path — read ~/.somewhere/config.json for the smt_ token like the CLI does; project id) at ~/.config/pretty/backup.json (0600). `pretty backup now` pushes: for each live/known session, its conversation file (claude JSONL / codex rollout, resolved via internal/watch) → PUT https://api.somewhere.tech/v1/fs/<project>/pretty-sessions/<machine>/<tool>/<uuid>.jsonl (raw bytes; the endpoint proven working: PUT /v1/fs/<project>/<path>). Also push a manifest.json (session id→name/cwd/tool/last-activity/path) so restore/recall knows what's there. `pretty backup status` shows last-push time + count.
2. Idempotent + incremental: skip files unchanged since last push (size+mtime cache). NEVER push secrets/env; only the conversation transcript files + manifest. Redact nothing extra (transcripts are the user's own data going to the user's own account) but DO respect a per-session opt-out flag.
3. Wire an optional periodic push (config interval) into the daemon lifecycle behind the enable flag — but keep it a simple timer, no new deps.
GATES: build/vet/full-suite green; test the push path against a MOCK http server (httptest) asserting method=PUT, path shape, bytes match a fixture conversation, manifest present, incremental skip works. Do NOT push real data to real somewhere in tests. docs/lanes/backup-NOTES.md real output. No commit.
