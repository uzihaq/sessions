# Lane: SOAKFIX — two cutover-blocking bugs from the live claude/codex soak
Work ONLY in /Users/uzair/pretty-PTY-soakfix. Both bugs reproduced live on the Go daemon tonight; both must ship with e2e proof. NEVER touch the running soak daemon on :8787 or default state — scratch daemon on a scratch port/state dir for all proofs (this machine has claude+codex authed; you may create REAL claude/codex sessions on your scratch daemon and kill them in cleanup).
BUG A — watcher resolution misses realpath'd cwds: a codex session created with cwd=/tmp writes rollouts with session_meta cwd=/private/tmp (macOS symlink) → internal/watch resolvers find no-cwd-match → pretty last/transcript return []. FIX: normalize BOTH sides with filepath.EvalSymlinks (fall back to Clean on error) wherever cwds are compared — codex resolver cwd match AND claude project-dir encoding (encode from the resolved path; ALSO probe the unresolved path's encoding for backward compat, mirroring the TS ambiguity rules in prettyd/src). Table tests incl the /tmp→/private/tmp case.
BUG B — `pretty send` to a CLAUDE session types but never submits: soak evidence: send returned the composer tail (unconfirmed), snapshot later contains NEITHER the prompt text NOR a reply; codex sessions submit fine. Normative: prettyd/bin/pretty.cjs's claude send path — study its exact submit sequence (text write, delay, CR, composer-cleared verification, retry/bracketed-paste handling) and port faithfully to the Go CLI/daemon input path. Diagnose first (reproduce on scratch: create claude session, send, snap) — the fix is whatever the real divergence is (likely Enter timing/encoding after text injection).
GATES: build/vet/full-suite -count=1 green ×3; E2E PROOF on your scratch daemon: (1) codex session in /tmp → send "Reply with exactly OK." → within 60s `pretty last <id> --json` returns a non-empty structured reply containing OK; (2) claude session in /tmp → send "Reply with exactly PONG." → snapshot contains PONG AND last --json non-empty. Paste real outputs in docs/lanes/soakfix-NOTES.md. No commit.
