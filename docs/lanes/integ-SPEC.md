# Lane: INTEGRATE go-daemon onto the merged go-rewrite tree
Work ONLY in /Users/uzair/pretty-PTY-integ (branch go-integrate, based on go-rewrite which already contains runner+mirror+watchers+contract). No commit.
JOB: bring the go-daemon branch's work (internal/api, cmd/prettyd, its state additions — see `git diff go-rewrite...go-daemon --stat`) into this tree. THE RUNNER'S internal/proto IS CANONICAL (it is interop-proven against the real TS daemon; see docs/lanes/runner-NOTES.md). Where the daemon branch defined its own proto/launcher interface, ADAPT the daemon code to the canonical proto (git checkout the daemon files, then port them; do NOT weaken the runner proto to fit the daemon).
Also WIRE the real pieces the skeleton stubbed: daemon's session create/attach should use the canonical proto client against real runner sockets, and snapshots should go through internal/mirror (replace any placeholder). Add ONE end-to-end Go test: start Go daemon on a scratch state dir + scratch port, exec the Go runner binary for `bash -i`, create/attach via the daemon HTTP API, send input `echo E2E_$RANDOM`, assert the snapshot endpoint returns it, kill session. That's the Go-daemon+Go-runner+Go-mirror round-trip — the first all-Go milestone.
GATES: CGO_ENABLED=0 go build ./... ; go vet ./... ; go test ./... ALL green including the daemon's existing auth-matrix tests (do not delete tests to pass) + the new e2e. NOTES → docs/lanes/integ-NOTES.md with real output. Scratch dirs only, never default state, never any real daemon.
