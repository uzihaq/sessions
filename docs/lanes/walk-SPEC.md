# Lane: WALK — onboarding walkthrough refreshed for Go/binary reality (board tsk_0068f6ac)
Work ONLY in /Users/uzair/pretty-PTY-walk. FILE OWNERSHIP: site/ ONLY (index.html, setup.html, connect.html, docs.html). Keep the phosphor-terminal design system EXACTLY (--ink/--cream/--phos vars, IBM Plex Mono + Instrument Serif, scanlines) — content edits only, no redesign, no external scripts (view-source-clean is a product guarantee).
UPDATE for the GO binary reality (the npm/node install story is GONE):
1. setup.html: install path is now `brew install uzihaq/tap/pretty` OR download the static binary (NO Node, NO npm, NO install scripts — call this out as the trust win). Then `pretty install` (sets up the launchd daemon) → open the printed URL. Keep the two paths (local vs from-anywhere via Tailscale) + the live daemon-reachability check (it already probes /api/health — keep, it works against the Go daemon identically). Troubleshooting: `pretty doctor`, no more node-pty/xcode step (binary has no native build). Uninstall: `pretty uninstall` (+ brew uninstall).
2. index.html: the "what we never do" trust contract stays; ADD the single-binary/no-install-scripts line as a trust point. Fix any node/npm references.
3. connect.html: unchanged mechanics (fragment endpoint+token, scrub) — just verify copy matches the Go reality; the hosted app (webapp lane) now lives here too, ensure the connect flow points at it.
4. docs.html: CLI reference refreshed to the Go command set (new/ls/send/ask/wait/run/lanes/status/verdict/recover/adopt/move/backup/remote/model/kill/doctor/install/uninstall) + hooks env.
GATES: HTML sanity (no broken tags, no external <script src=http>), grep confirms zero remaining `npm i`/`node-pty` install instructions. docs/lanes/walk-NOTES.md lists changes. No commit.
