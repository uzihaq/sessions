# Lane: PARITY — prove the React frontend + WS streaming work unchanged on the Go daemon
Work ONLY in /Users/uzair/pretty-PTY-parity. FILE OWNERSHIP: create ONLY prettygo/parity/ (harness code, Node ok) + docs/lanes/parity-NOTES.md. Do NOT modify daemon/session/api source — if you find a divergence you cannot test around, DOCUMENT it precisely (file:line, expected-vs-actual) as a finding; fixes belong to another lane.
SCOPE: side-by-side behavioral diff, TS daemon vs Go daemon, each on its own scratch state dir + port:
1. HTTP parity: for the same action sequence (create bash session, ls, input, snapshot, tail, kill) capture responses from both daemons; diff JSON SHAPES (field names/types; values like ids/timestamps normalized). Report per-endpoint verdict.
2. WS parity: attach a WS client (Node, using frontend's message expectations — read frontend/src to learn the protocol it consumes) to both daemons' terminal stream for a live session; drive input; diff the message TYPE sequences + payload shapes (not raw bytes).
3. REAL FRONTEND smoke: serve frontend/dist from the GO daemon (it already serves static), and with a headless browser (puppeteer available in frontend/node_modules) load the UI against the Go daemon, create a session via the UI's API path, assert the terminal renders output (screenshot + DOM text check for a marker echoed in bash).
GATES: parity report table in docs/lanes/parity-NOTES.md (endpoint/message → identical|divergent w/ details) + screenshot path + the marker found in DOM. Findings list for any divergence. No commit. Scratch only, never default state, never real daemons.
