# Lane: WATCHERS — claude JSONL + codex rollout watchers/resolver in Go
Work ONLY in /Users/uzair/pretty-PTY-watchers. Read prettygo/ARCHITECTURE.md — pins are law. Normative: prettyd/src/claudeWatcher.ts (or equivalent — find the claude JSONL watcher), codexResolver.ts, codexWatcher.ts, plus their tests (prettyd/scripts/test-codex-resolver.mjs, test-codex-backfill.mjs). Build prettygo/internal/watch/. No commit.
SCOPE — port SEMANTICS EXACTLY, they encode hard-won lessons: (1) claude JSONL: session→file resolution rules incl the ambiguous-empty-by-design case; tail→normalized event structs (match the event shapes the API serves). (2) codex resolver: rollout discovery with 64KiB first-line read cap (16KiB truncation was a real prod bug — keep 64), date-window {today,yesterday,createdAt} then FULL-SCAN fallback, resume-uuid global match first; same reason strings (fresh-match, fresh-match-fullscan, no-cwd-match, resume-match...). (3) codex watcher: tail with BACKFILL on attach — replay existing content bounded (~last 2000 lines) through the same emit path, exact byte-offset handoff to live tail (no gap, no dup). fsnotify as wake hint + poll fallback; never trust events alone.
ACCEPTANCE (NOTES.md, real output): Go table tests with synthetic fixtures reproducing the TS test cases: resolver (incl a >16KiB first-line fixture proving the 64KiB cap, an out-of-window fixture proving full-scan) and backfill (replay N events then append→emitted exactly once). go vet + build clean. Do not read any real ~/.codex or ~/.claude data — synthetic only.
