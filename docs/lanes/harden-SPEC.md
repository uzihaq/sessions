# Lane: CUTHARDEN — close the two MINI-SWAP blockers from the opus audit
Work ONLY in /Users/uzair/pretty-PTY-harden. FILE OWNERSHIP: internal/session/manager.go (+ its tests), internal/interop/ (extend cutover_test.go), cmd/prettyd/main.go (ONLY the discovery-scheduling line). Do NOT touch api handlers/auth/mirror/ledger (other lanes own those). Read docs/CUTOVER_AUDIT_2026-07-17.md findings #1 and #3.
FINDING #3 — transiently-lost-but-alive runner stranded (manager.go:734 scheduleReconnect early-returns at ~:745 when registry.Get still present, killing the backoff chain; Discover runs once at startup only):
  FIX: (a) scheduleReconnect must ALWAYS keep the backoff chain alive when the runner's socket is unreachable but the runner process/plist still exist — do not bare-return; re-arm the timer. (b) Add a PERIODIC guarded re-discovery: a daemon ticker (default 30s, configurable via env) that runs the SAME guarded Discover path (mass-kill guard intact — must never reap a live runner; only reattach lost-but-alive ones and classify genuinely-dead ones exactly as startup Discover does). Wire the ticker in cmd/prettyd/main.go. Prove: a test that connects a runner, forces a socket blip (close the daemon-side conn without killing the runner), and asserts the session reappears via periodic discovery WITHOUT a full daemon restart; and that a live runner is never reaped by the periodic pass.
FINDING #1 — interop coverage too narrow: extend internal/interop to also prove (a) a CLAUDE runner (real claude, this machine is authed; scratch state + scratch label; kill in cleanup) adopted by the Go daemon after restart RE-RESOLVES its JSONL and resumes structured events (pretty last returns the history); if codex is fast enough add codex too; (b) a SCALE case: N>=5 runners adopted at once, all attached, none reaped; (c) an ORPHAN case: create >3 genuinely-dead runner artifacts (metadata+plist, no live process) alongside live ones and assert Discover reaps ONLY the dead and the mass-kill guard is exercised (force path vs guarded path). Keep the existing bash cutover + reverse-regression tests.
GATES: CGO_ENABLED=0 build/vet green; full suite -count=1 green; the new interop + reconnect tests green (run them yourself, paste output). docs/lanes/harden-NOTES.md. No commit. Scratch state + scratch launchd labels ONLY (never tech.pretty-pty.daemon, never the mini), kill only by exact scratch PID/label.
