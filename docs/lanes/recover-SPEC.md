# Lane: RECOVER+ADOPT — finish the ledger story (board tsk_64772bd2)
Work ONLY in /Users/uzair/pretty-PTY-recover. FILE OWNERSHIP: internal/ledger/* (extend), internal/recovery/ (new), internal/api (ONLY new route registrations + new handler file recovery_handlers.go), cmd/pretty/recover.go + adopt.go (new files) + ONE dispatch line each in commands.go.
BUILD: (1) reconciliation engine internal/recovery: fold ledger events + observe reality (state-dir metadata, socket HELLO probe, launchd print, conversation-store existence via internal/watch resolvers) → per-lane classification {live-managed|closed|unexpectedly-lost|external} + anomalies + recency ranking (activity events, human-input time preferred). (2) GET /api/recovery → the classified report. (3) `pretty recover` prints the by-name plan (name, tool, cwd, last-activity, resume recipe); `pretty recover --reopen` executes create-with-resume for every unexpectedly-lost lane (idempotent: skip if a live lane already has that provider uuid; append reopened events). (4) `pretty adopt <path-or-uuid>`: EXPLICIT adoption of an external claude jsonl / codex rollout → resolve cwd+uuid, create-with-resume, write created+provider_bound(actor=adopt). NEVER auto-adopt; provider-unbound refusal per design.
GATES: build/vet/full-suite green; e2e test: scripted scenario in scratch — create 3 lanes via manager (fake launcher), tombstone one via kill, orphan one (delete its runner state, emit runner_lost), leave one live → recover report classifies 3 ways correctly; --reopen resurrects exactly the orphan. docs/lanes/recover-NOTES.md real output. No commit.
