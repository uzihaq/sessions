# Lane: STABILIZE — the whole suite deterministic under load, once and for all
Work ONLY in /Users/uzair/pretty-PTY-stabilize. MANDATE: `CGO_ENABLED=0 go test ./... -count=1` must pass 10 CONSECUTIVE runs (script the loop, paste full output). Known residual load-flakes surface only in full-suite runs (isolated packages pass): at least one in internal/session (recent failure at 2.594s under load) and historically internal/waitcond timing tests. METHOD per failure: reproduce under load (run full suite in a loop, optionally with -parallel high / while `go test ./internal/mirror -count=5` runs concurrently to add load); NAME the test; diagnose PRODUCT race vs TEST timing (race detector run REQUIRED: go test ./... -race -count=1 at least twice); product races get product fixes, test-timing gets observable-state synchronization or a controllable clock — NEVER weakened assertions, NEVER longer sleeps as the fix, NEVER t.Skip. Also: audit remaining wall-clock sleeps in _test.go files (grep 'time.Sleep') and eliminate where they gate assertions.
GATES: the 10×-green loop output + 2× race-detector green in docs/lanes/stabilize-NOTES.md with per-flake root-cause paragraphs. No commit.
