# Lane: CONTRACT — extract the normative protocol docs the whole Go port builds against
Work ONLY in /Users/uzair/pretty-PTY-contract. Read prettygo/ARCHITECTURE.md first — its pins are law. Deliverables under prettygo/CONTRACT/. TS source (prettyd/src/, prettyd/bin/, frontend/src/api/ or lib/prettyd*.ts) is normative — READ it, document what IS, never what should be. No commit.
1. http-api.md — EVERY route in prettyd/src/http.ts: method, path, query/body params, response JSON shape (real field names/types), status codes, auth requirements (token/health exemptions), origin rules (config.ts isAllowedOrigin incl. HOSTED_SHELL_ORIGINS).
2. ws.md — WS upgrade path+params (prettyd/src/ws.ts) and every message type in BOTH directions incl. how the frontend consumes them (frontend/src — find the ws client).
3. runner-protocol.md — the unix-socket frame protocol between daemon and runner (prettyd/src/runner.ts + the daemon-side client): exact byte framing (length prefix endianness/width, seq/type fields), every frame type with payload, handshake (HELLO), resize/input/kill semantics, and the .events on-disk framing (same? document exactly).
4. state-dir.md — ~/.local/state/pretty-PTY layout: {id}.json metadata schema (every field), .sock, .events, .log, idle/<id> sentinels, token file, open file, launchd plist template + label scheme (find plist generation in sessions.ts), discovery algorithm on daemon start.
5. fixtures/ — concrete examples: a valid runner .json, a hex dump of a few .events frames w/ annotation, sample HTTP responses (run the built TS daemon on a SCRATCH dir to capture real ones: PRETTYD_STATE_DIR=/tmp/ct-state PRETTYD_PORT=8899 node prettyd/dist/server.js — NEVER default state dir).
Acceptance in NOTES.md: file list + how each was verified against source. Exhaustive > pretty.
