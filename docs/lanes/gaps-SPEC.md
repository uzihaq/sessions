# Lane: GAPS — exhaustive route sweep: port EVERY remaining TS daemon route to Go
Work ONLY in /Users/uzair/pretty-PTY-gaps. Method: build the COMPLETE route table from prettyd/src/http.ts + ws.ts (every method+path, incl query params), diff against prettygo/internal/api registered routes, implement EVERY missing one with identical shapes. Known missing from the parity browser smoke: GET /api/fs/list (directory picker backend) and GET /api/claude-sessions (claude conversation enumeration for resume pickers — reuse internal/watch's claude resolver). There may be MORE — the sweep is the point: list the full table with status ported/missing→implemented in your NOTES.
Constraints: shapes identical to TS (field names/types); fs/list must be path-sanitized exactly like the TS impl (no traversal); claude-sessions reads ~/.claude read-only — for TESTS use a fixture HOME. Pins in ARCHITECTURE.md law.
GATES: CGO_ENABLED=0 build/vet/full-test -count=1 green; new table tests per added route (shape + a traversal-rejection test for fs/list); re-run the parity harness's HTTP checks if runnable. docs/lanes/gaps-NOTES.md: the complete route table + real output. No commit.
