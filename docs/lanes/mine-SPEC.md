# Lane: PROVENANCE + `pretty lanes --mine` (board tsk_3716462f, codex-designed — implement the adopted design)
Work ONLY in /Users/uzair/pretty-PTY-mine. FILE OWNERSHIP: internal/ledger (created-event fields + fold), internal/session (env injection + capture on create), cmd/pretty (CLI forward + --mine/--owner/--all/--subtree/--direct). Solves: agents forget which lanes they opened (compaction wipes memory) → query instead of remember.
ADOPTED DESIGN (provenance graph, NOT auth):
1. Add IMMUTABLE creator_kind (session|user|external) + creator_id to the ledger `created` event + folded state. session→creator session UUID (expose as parent_session_id); user→"uid:<daemon-uid>"; external→caller-supplied owner (--owner/PRETTY_OWNER_ID, opaque).
2. CAPTURE at CLI/daemon boundary: the daemon injects PRETTY_SESSION_ID=<new-session-uuid> into each launched session's env (AFTER merging caller env so it can't be overridden — NOTE: code currently injects RUNNER_ID and only puts PRETTY_SESSION_ID in idle-hook env; wire it into the main session env). The CLI reads PRETTY_SESSION_ID from its OWN env and forwards it on create as header X-Pretty-Creator-Session; daemon validates the UUID has a created event and commits it in the child's write-ahead created event. Invalid provenance ERRORS (never silently 'user').
3. `--mine` resolution precedence: --owner/PRETTY_OWNER_ID → external:<id>; else PRETTY_SESSION_ID → session:<uuid>; else → user:<uid>. Session selector returns lanes whose creator ANCESTRY contains that session (transitive SUBTREE by default; --direct = immediate children only). User/external → lanes rooted at that principal (so a plain terminal sees user-rooted work, not every authenticated lane). Reject simultaneous inherited-session + explicit --owner unless --detach.
4. Store DIRECT edges (A→B→C); derive roots (do NOT persist root_session_id). Dead parent → descendants classified 'parent-dead', never unowned, never auto-reassign. Keep `lanes`/`ls`/`--all` as global view; add --subtree <id>.
5. Provenance IMMUTABLE (append-only created). NO adoption/ownership-transfer in v1 (defer).
GATES: CGO_ENABLED=0 build/vet/full-suite green; tests: direct child, nested (A→B→C subtree vs --direct), missing/forged/stale parent id, user fallback (no PRETTY_SESSION_ID), external --owner isolation, dead-parent classification, --mine descendant filtering. docs/lanes/mine-NOTES.md. No commit. Scratch only.
