# Lane: MODELCAT — model catalog + validated model/effort/serviceTier over the app-server (board tsk_e06df6ee)
Work ONLY in /Users/uzair/pretty-PTY-modelcat. FILE OWNERSHIP: internal/codexapp (add listModels + validation), internal/session (thread the choice through app-server session create), cmd/pretty (--model/--effort/--fast validation for codex-app-server sessions). Read internal/codexapp/*.go (the merged client) + the protoschema.
CONTEXT: the codex app-server client (internal/codexapp) is merged + live. NewConversation already accepts model/effort. Now add the CATALOG + VALIDATION so a declared model/effort is checked against the LIVE server catalog and fails clearly if unavailable — NEVER a silent downgrade (the "declared-requirements-with-validation" design, resolved earlier: models aren't pinned packages; the catalog is server-supplied + rotates).
BUILD:
1. internal/codexapp: ListModels(ctx) -> []Model{id, displayName, supportedEfforts?, ...} via the app-server protocol (find the list-models / model-catalog method in protoschema/*.json; if none exists, document precisely what IS available — e.g. models surface only via a config/newConversation error — and validate by attempting + mapping the structured error). ResolveModelChoice(catalog, {model, effort, serviceTier}) -> resolved | clear error listing valid options.
2. Wire into codex-app-server session create: `pretty new --tool codex --model X --effort Y [--fast]` validates X/Y against ListModels() BEFORE NewConversation; on mismatch, fail with "model X not available; valid: [...]" (exit nonzero, never downgrade). service_tier=priority mapping preserved from the PTY path semantics.
3. `pretty models` (or `pretty codex models`) — print the live catalog (--json too). Small, useful.
GATES: CGO_ENABLED=0 build/vet/full-suite green; unit tests for ResolveModelChoice (valid, invalid-model, invalid-effort → clear errors, no silent downgrade); a GATED integration test (CODEXAPP_INTEGRATION=1 style) that ListModels returns a non-empty catalog from the real app-server and an invalid model is rejected. docs/lanes/modelcat-NOTES.md with the real catalog output. No commit. Scratch only.
