# Lane: MOVE — pretty move <session> --to <machine> (fleet phase 2, board tsk_f4039d1a)
Work ONLY in /Users/uzair/pretty-PTY-move. FILE OWNERSHIP: internal/migrate/ (new), cmd/pretty/move.go (new) + one dispatch line in commands.go, api handler in NEW file move_handlers.go + route registration, and ADD a ledger event type "moved_to"/"moved_from" (additive to internal/ledger event enum — coordinate: only ADD, don't restructure).
DESIGN (from the fleet epic — resume-elsewhere, NOT process migration): a session = {conversation file, workspace (git), resume recipe}. `pretty move <session-id> --to <target-endpoint> [--token T] [--dry-run]`:
1. Resolve source session (cwd, tool, provider uuid, resume recipe) from the local daemon + ledger.
2. WORKSPACE: if cwd is a git repo, require it pushed (or --allow-dirty → create a ledger checkpoint ref) and report the remote+branch the target must have; the target clones/checks out (document the contract — for v1 ASSUME the target can reach the same git remote; move syncs the CONVERSATION, not the repo bytes).
3. CONVERSATION: copy the conversation file (claude JSONL / codex rollout, resolved via internal/watch) to the target machine's conversation store via the target daemon (add POST /api/migrate/receive on the daemon that accepts {tool, uuid, cwd, conversation_bytes, resume_recipe} and writes the conversation file into the right place so a subsequent create-with-resume finds it). Auth via the target's token.
4. CREATE on target: call the target daemon's create with the resume recipe → the session resumes there with full history.
5. SOURCE: write ledger moved_to (with target endpoint + new id), and OPTIONALLY kill the source (default: leave source, print "source still live; kill with pretty kill <id> once verified" — sessions are sacred, don't auto-kill on move).
GATES: build/vet/full-suite green; e2e in scratch: TWO Go daemons on scratch ports/state (A and B); create a bash session on A with a marker in its cwd; pretty move it A→B; assert B lists the session and (for a claude/codex you may skip live-tool; for shell, assert the resumed session on B is live and the conversation/metadata transferred); ledger on A has moved_to. docs/lanes/move-NOTES.md real output. No commit. Scratch only.
