# Lane: CLI — port pretty.cjs + remote.cjs to Go (cmd/pretty)
Work ONLY in /Users/uzair/pretty-PTY-cli. Normative: prettyd/bin/pretty.cjs (2141L, read ALL) + bin/remote.cjs (345L). Build prettygo/cmd/pretty/ (stdlib flag parsing or a tiny internal arg helper — NO cobra, keep deps lean). Pins in ARCHITECTURE.md law. No commit; never touch real daemons/state.
SCOPE — every command, same flags, same output shapes (agents parse these): new (tool presets + --model/--effort/--fast validation semantics), ls, send (PORT THE EVIDENCE-TIER CONFIRMATION exactly — decideSendConfirmation tiers confirmed/accepted/unconfirmed exits 0/0/1/2 + confidence in --json; it's in pretty.cjs ~line 340-370, freshly fixed), wait (incl idle semantics), last/transcript (--json shapes identical), model, kill, doctor, version, install (launchd daemon plist — scratch-label test only), deploy (port the safe-deploy steps incl PRETTYD_SMOKE preflight), remote enable/status/disable (port remote.cjs incl the honest MagicDNS diagnostics + QR via a pure-Go qr lib), snap, resize.
PRETTYD_HOST/PORT env + --host handling identical. Human output byte-compatible where agents grep it (ls columns, send confirmation lines); --json fields exactly matching.
GATES: build/vet green; a table test for send decision tiers; golden tests comparing `pretty ls` / `send --json` output SHAPES against fixtures captured from the node CLI run against a scratch TS daemon (node CLI is runnable: prettyd/bin/pretty.cjs). docs/lanes/cli-NOTES.md real output. No commit.
