# Lane: POLISH-1 — fix the build + daily-driver install on THIS MacBook
Work ONLY in /Users/uzair/pretty-PTY-polish. Two items:
1. BUILD BUG: `make binaries` (prettygo/Makefile + scripts/build-binaries.sh) fails with `tsc: command not found` (Error 127) at the frontend-embed step. Root cause: the frontend build invokes bare `tsc`/`vite` instead of resolving them from frontend/node_modules/.bin. FIX: build the frontend via `npm --prefix frontend run build` (or set PATH to frontend/node_modules/.bin) so tsc/vite resolve; ensure the embedui flow stages frontend/dist into the binary. Prove: `make binaries` succeeds from a clean shell (PATH without global tsc) and the darwin binary serves the embedded UI (curl / has <title>). Also make the frontend build step SKIPPABLE (make binaries-noui) for fast Go-only iteration.
2. DAILY-DRIVER INSTALL on the MacBook (dev, MUST NOT collide with anything): add/verify `pretty install` sets up the GO daemon as a launchd service under a DISTINCT dev label (e.g. tech.pretty-pty.dev.daemon — NOT tech.pretty-pty.daemon which is the mini's prod label; confirm the label is configurable + defaults safe) with KeepAlive+RunAtLoad, pointing at the installed Go binary + PRETTYD_RUNNER=the runner binary, on a chosen port (default 8787 ok on the MacBook since nothing else uses it here). `pretty install` should: build/locate binaries, write the plist, bootstrap it, wait for health 200, print the URL. `pretty uninstall` cleanly reverses. Idempotent. Do NOT actually bootstrap during the test — unit/dry-run the plist generation (assert label, ProgramArguments point at the go binary, KeepAlive/RunAtLoad true, port/PRETTYD_RUNNER env present) + a scratch-label integration test that bootstraps→health→boots out in cleanup.
GATES: CGO_ENABLED=0 build/vet/full-suite green; `make binaries` succeeds clean; plist-gen tests green. docs/lanes/polish-NOTES.md real output incl a successful `make binaries` transcript. No commit. Never touch tech.pretty-pty.daemon or the mini.
