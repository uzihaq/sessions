# Lane: FLEET — see lanes across machines (roadmap tsk_f4039d1a phase 1)
Work ONLY in /Users/uzair/pretty-PTY-fleet. FILE OWNERSHIP: frontend/ ONLY (React). The multi-server store already exists (frontend/src/lib/servers.ts — scheme/token/endpoint). Build the FLEET VIEW: aggregate live sessions across ALL configured servers into one list grouped by machine/server (server name header, its sessions beneath), with per-session state (working/idle/exited) and a machine-reachability indicator (green/red dot from each server's /api/health). Each server polled independently; a down server shows its group greyed with "unreachable", never blocks others. Reuse existing session-row components. Add a fleet/grid toggle. Zero new trust surface: the browser talks to each daemon directly (dumb pipe intact); no cross-daemon calls, no new backend.
GATES: npx tsc --noEmit clean + npx vite build clean; a note in docs/lanes/fleet-NOTES.md describing the component + manual trace (2 servers, one unreachable → its group greyed, other still live). If a test harness exists use it. No commit.
