# Lane: SESSIONS — port sessions.ts (lifecycle, launchd, classifier, notifications) to Go
Work ONLY in /Users/uzair/pretty-PTY-sessions. Normative: prettyd/src/sessions.ts (1287L — read ALL of it) + push.ts + docs/lanes/contract's state-dir.md. Build prettygo/internal/session/ (+ extend internal/state as needed, do not fork it). Pins in prettygo/ARCHITECTURE.md are law. No commit. NEVER touch default state dirs, real launchd labels outside a scratch prefix, or any real daemon.
SCOPE (port SEMANTICS exactly — this file encodes a year of lessons):
1. createSession: metadata write → launchd plist gen + bootstrap (plist template per contract docs; for TESTS use a scratch label prefix like tech.pretty-pty-test.runner.* and bootout in cleanup), withToolDefaultArgs (codex/claude skip-perms injection, claude --session-id pin), fixed 300x50.
2. discoverRunners on startup: state-dir scan, socket HELLO probe, reattach, orphan handling — CRITICAL: port the sacred-sessions rules; NEVER reap on transient unreachability; include the MASS-KILL GUARD (refuse >N bootouts per sweep absent an explicit force flag) — this is NEW, board tsk_64772bd2 context, wire it into every kill path.
3. Working-state classifier: spinner/footer detection driving working true/false (the debounced edge), pushWorkingObserved gating, writeIdleSentinel/remove, runOnIdleHook + runGlobalOnIdleHook (env PRETTY_SESSION_ID/NAME/TOOL/CWD/PRETTY_FINAL_MESSAGE/PRETTY_OUTCOME/PRETTY_DURATION_MS), classifyIdleReason (done/blocked/error on mirror snapshot — port the regexes exactly) and finalAssistantSummary.
4. Push notifications: port push.ts semantics via github.com/SherClockHolmes/webpush-go (pure Go — verify no cgo); same VAPID key file locations; the 🟢/🟡/🔴 title/body shapes from sessions.ts.
5. Wire into internal/api so create/kill/list use this for real (replacing remaining stubs), keep ALL existing tests green.
GATES: CGO_ENABLED=0 build/vet/test green incl existing suites + NEW tests: classifier table (the 5 snapshot cases from prettyd's test-idle-notifications heritage), mass-kill guard test, a launchd-free createSession test (plist written to scratch, content asserted). docs/lanes/sessions-NOTES.md w/ real output. No commit.
