# Lane: INTEG-ENDPOINTS — OUR side of the somewhere integrations (endpoints + shapes ONLY)
Work ONLY in /Users/uzair/pretty-PTY-integ-endpoints. FILE OWNERSHIP: internal/integrations/ (new), api handlers in NEW file integrations_handlers.go + route registrations, cmd/pretty additions in NEW files recall.go (thin), docs/INTEGRATIONS.md. Do NOT build any somewhere MCP tool or call somewhere APIs — build OUR endpoints + documented JSON shapes that somewhere's MCP/integrations will consume. The contract is the deliverable.
1. SESSION-HISTORY RECALL ENDPOINT (pairs with the shipped backup): GET /api/history — list sessions with recall metadata {id, name, tool, cwd, machine, created_at, last_activity_at, message_count, conversation_available: bool}. GET /api/history/:id?format=json|text → the session's normalized transcript (reuse internal/watch normalization) as a STABLE documented shape {session:{...}, messages:[{role, text, timestamp}]}. GET /api/history/:id/raw → the raw conversation file bytes. Document every shape in docs/INTEGRATIONS.md with example responses — THIS is what a somewhere MCP "recall" tool calls. Auth: normal token.
2. ERROR-REPORT EMIT ENDPOINT + FEED: the daemon already detects failures (runner crash, dep errors). Add an internal error-event recorder (internal/integrations) that captures structured {ts, kind, session_id?, summary, detail, machine} into an append-only errors.jsonl in state dir, AND exposes GET /api/errors?since=seq → {errors:[...], nextSeq} (a documented feed a somewhere integration polls to auto-file tasks — WE don't file tasks, we EXPOSE the feed). Wire a few real emit points (runner exit nonzero, daemon-level caught errors). Document the shape in INTEGRATIONS.md.
3. Make both shapes VERSIONED (schemaVersion) and stable.
GATES: build/vet/full-suite green; httptest coverage: /api/history list + one transcript (fixture conversation) shape asserted; error feed: emit 2 errors → /api/errors returns them with nextSeq paging. docs/INTEGRATIONS.md complete with example JSON. docs/lanes/integ-endpoints-NOTES.md. No commit. Scratch only.
