# Lane: LANES — headless runs as first-class sessions (pretty run)
Work ONLY in /Users/uzair/pretty-PTY-lanes. FILE OWNERSHIP: internal/session (extend Manager for a new session kind), internal/state (metadata field), cmd/runner (headless mode), cmd/pretty/run.go + lanes.go (new), api handler additions in a NEW file lanes_handlers.go + route/dispatch one-liners. The dogfooded spec (board tsk_193b35dc) — the orchestrator's own pain list:
1. `pretty run [--name N] [--cwd D] [--spec FILE] -- <cmd args...>`: launches a HEADLESS lane as a real session: launchd-owned runner in pipe mode (no PTY; capture stdout+stderr merged into the .events stream with the same framing), metadata kind:"lane", registered in the ledger (created/launch_started/runner_exited events), visible in `pretty ls` (TOOL column shows lane:<basename>), snapshot returns the output tail, kill works. If --spec given, record its path in metadata.
2. COMPLETION MANIFEST: on child exit the runner writes {exit_code, signal, duration_ms, last_output_tail (bounded 4KB), spec_path, files_changed?: git -C cwd status --porcelain count if cwd is a repo} to <id>.manifest.json in the state dir; `pretty last <lane-id> --json` returns it; ledger gets runner_exited with exit_code in payload.
3. NOTIFY-ON-DEATH: nonzero exit or signal → the daemon's existing push path fires 🔴 "<name> died (exit N)" with the last output line as body; clean exit → 🟢 "<name> finished". Reuse the notification machinery from internal/session; coalesce: if ≥3 lanes die within 60s with similar exit, one digest push "N lanes died".
4. `pretty lanes` = `pretty ls --kind lane` shorthand w/ manifest columns (EXIT, DURATION). `pretty wait <lane-id>` (existing wait) must treat lane exit as the terminal event; `wait --any` composes (that lane's code is merged — integrate, don't duplicate).
GATES: build/vet/full-suite green ×3; e2e: pretty run a script that sleeps 2s, echoes MARKER, exits 3 → ls shows lane, wait returns on exit, last --json has exit_code 3 + MARKER in tail, manifest file valid, ledger has the events. docs/lanes/lanes-NOTES.md real output. No commit. Scratch state only.
