# Lane: CUTOVER — prove the mini swap is safe + write the runbook
Work ONLY in /Users/uzair/pretty-PTY-cutover. This gates a LIVE swap of the Mac mini's daemon (node→Go) UNDER ~30 real sessions. THE decisive unknown: the mini's runners were spawned by the NODE runner (prettyd/src/runner.ts); can the GO daemon discover + attach to a NODE-spawned runner over the existing unix socket + frame protocol? Interop was proven the OTHER way (Go runner under TS daemon). Prove THIS direction or the cutover is unsafe.
1. INTEROP PROOF (internal/state or a new internal/interop test, Node allowed): start the NODE runner (node prettyd/dist runner path — study how sessions.ts launches it; replicate its env RUNNER_* on a scratch state dir + a scratch launchd label prefix or direct spawn) for `bash -i`; then start the GO daemon on that same scratch state dir; assert the Go daemon DISCOVERS the node runner, attaches, GET /api/sessions lists it, input round-trips (echo marker → snapshot), and it survives. If frame/HELLO mismatches surface, DOCUMENT precisely (this is a real finding — may require a small Go-daemon compat shim; implement it if contained to the daemon side, else document for a follow-up). Also test the reverse already-known-good as a regression.
2. docs/CUTOVER.md runbook: preflight checklist (Go suite green, interop proof green, binaries built for darwin-arm64, mini reachable, a FRESH mini backup taken); the swap (stop node launchd daemon, install Go daemon plist pointing at the Go binary + PRETTYD_RUNNER, kickstart, health 200, verify runner count unchanged + a live session round-trips); ROLLBACK (repoint launchd at node dist, kickstart — runners survive both ways); post-cutover checks; and `pretty recover` as the on-mini safety net.
3. scripts/cutover.sh + scripts/rollback.sh: parameterized (PRETTYD_HOST), DRY-RUN default, each step printed + guarded (runner-count baseline, abort on drop), never destructive without --execute. Do NOT run against the mini — dry-run against the local soak daemon only.
GATES: interop test green (CGO_ENABLED=0), full suite green, cutover.sh --dry-run output pasted, CUTOVER.md complete. docs/lanes/cutover-NOTES.md. No commit. Scratch only, NEVER the mini (100.86.76.84).
