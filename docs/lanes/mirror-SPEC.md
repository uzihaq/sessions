# Lane: MIRROR — terminal emulation + snapshot/serialize/reflow parity (the risk spike)
Work ONLY in /Users/uzair/pretty-PTY-mirror. Read prettygo/ARCHITECTURE.md — pins are law (pure Go). Normative behavior: what prettyd does with @xterm/headless + @xterm/addon-serialize + its reflow engine (find reflow code in prettyd/src/ — reflow/snapshot modules). Build prettygo/internal/mirror/. No commit.
GOAL: Mirror type: Write(raw PTY bytes) maintaining a 300x50 screen; Snapshot() → plain-text viewport; SerializeANSI() → ANSI string that reproduces the screen (like addon-serialize); ReflowTo(width) → the server-side ANSI-aware reflow the TS daemon serves to narrower clients (match its semantics: wrapping prose, preserving hard newlines — read the TS reflow source and match).
APPROACH: first EVALUATE pure-Go VT libs (hinshun/vt10x, charmbracelet/x/vt or similar, danielgatis/go-vte bindings NO if cgo) — document choice + why in NOTES; wrap one if adequate, else implement the needed subset (CSI cursor/erase/SGR colors incl 256+truecolor, scroll region, alt screen, wrapping).
ACCEPTANCE (oracle diff, real output in NOTES.md): build a Node ORACLE script using prettyd's own node_modules (@xterm/headless + addon-serialize, same 300x50) and a Go harness; feed BOTH the same recorded byte streams: (a) scripted PTY recordings you generate (bash colored ls, vim opening a file, a curses-style redraw, long soft-wrapped prose, emoji/CJK) captured via creack/pty or `script`; (b) at least one recording of a real `codex` or `claude` TUI startup if runnable here. Diff snapshot text + serialized-ANSI-rendered-back. Report parity per case; document every divergence honestly with the byte sequence that causes it. Target: 100% on snapshot text; serialize may differ in escape encoding if re-feeding it reproduces the same screen (prove THAT instead).
