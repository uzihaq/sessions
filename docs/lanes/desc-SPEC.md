# Lane: DESC — a description of WHY a lane/session exists (user daily-use need)
Work ONLY in /Users/uzair/pretty-PTY-desc. WHY THIS EXISTS: at cleanup time (pretty sessions --mine), the user/agent can't tell WHY a lane exists → hard to decide what's safe to kill. Give every session/lane an optional purpose description.
FILE OWNERSHIP: internal/ledger (add description to the created event + folded state — additive), internal/state (metadata field), cmd/pretty (new.go/run.go accept --description; ls.go/lanes.go/sessions.go/status.go DISPLAY it). Do NOT touch search files (another lane owns those).
BUILD:
1. `pretty new ... --description "..."` and `pretty run ... --description "..."` (accept --desc as alias) — store the description in session metadata + the ledger `created` event (immutable, part of provenance). 
2. DISPLAY it where cleanup decisions happen: `pretty sessions`/`lanes`/`ls` show a DESC column (truncated) — most important in `sessions --mine` (the cleanup view); `pretty status` shows the full description; --json includes a `description` field.
3. FALLBACK (do this simply, it's the point): if no --description was given, derive a best-effort one from the FIRST user message sent to the session (first ~80 chars, once a first message exists) so even un-described lanes show something meaningful at cleanup. Store the derived value separately (description_source: "explicit"|"first-message"|none) so an explicit description always wins and the derivation never overwrites it.
GATES: CGO_ENABLED=0 build/vet/full-suite green; tests: --description persists + shows in sessions/status/--json; first-message fallback populates description_source=first-message when no explicit desc; explicit desc wins over fallback. docs/lanes/desc-NOTES.md. No commit. Scratch only.
