# Lane: APIFUZZ — harden internal/api (the daemon's external HTTP surface — last major un-fuzzed package)
Work ONLY in /Users/uzair/pretty-PTY-apifuzz. FILE OWNERSHIP: internal/api (add *_test.go fuzz/property/adversarial tests; fix PRODUCT bugs found ONLY here). This is the daemon's EXTERNAL surface — a crash/hang/panic here = the whole daemon down (though net/http recovers handler panics, a panic still aborts that request + logs; hangs/unbounded reads are worse). Adversarial targets:
- FuzzRequestRouting / handler robustness: malformed request bodies (truncated JSON, wrong types, huge/negative numbers, deeply nested, non-UTF8), oversized bodies vs the JSON size cap, bad query params (huge tail=, negative before=, non-numeric), path traversal attempts on fs/list + static + upload (../, encoded, symlink — assert containment holds), missing/garbage auth headers, weird Origin values (assert the allowlist + Vary hold), malformed WS upgrade requests. Must never panic, never unbounded-read, never leak beyond home for fs; malformed → clean 4xx.
- A concurrency/-race test: many concurrent requests (create/list/kill/snapshot) against a scratch daemon with a fake runner, asserting no data race on the session registry/handlers.
A crash/hang/traversal-escape = PRODUCT bug → fix in-package + seed corpus. NO weakened assertions, NO skips. If net/http already recovers a panic, still fix the panic (a handler shouldn't panic on untrusted input).
GATES: CGO_ENABLED=0 build/vet green; go test ./internal/api -race -count=1 green; fuzz targets -fuzztime=45s NO crash (paste output); full suite green. docs/lanes/apifuzz-NOTES.md with any bug + seed. No commit. Scratch only, never the installed :8787 daemon.
