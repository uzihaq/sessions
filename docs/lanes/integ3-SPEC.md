# Lane: INTEG3 — land go-integ-endpoints onto current go-rewrite (server.go route seam)
Work ONLY in /Users/uzair/pretty-PTY-integ3 (branch go-integ3 from go-rewrite). The go-integ-endpoints BRANCH added /api/history* and /api/errors routes + handlers (internal/integrations, integrations_handlers.go, cmd/pretty/recall.go, docs/INTEGRATIONS.md). `git merge go-integ-endpoints` conflicts ONLY in prettygo/internal/api/server.go (the route-registration mux — both it and the recently-merged authpush touched it). RESOLUTION: keep BOTH sets of routes. Take current HEAD's server.go as base, ADD the integrations route registrations (history list/get/raw, errors feed) from the go-integ-endpoints side. The handler FILES (integrations_handlers.go, internal/integrations/*, INTEGRATIONS.md, recall.go) merge clean — only the mux registration needs hand-resolution. Verify no duplicate route paths, no dropped authpush routes.
GATES: CGO_ENABLED=0 build/vet/full-suite -count=1 green; the integrations httptest coverage (history shape + errors feed paging) green; go-rewrite's existing api/auth/push tests still green. docs/lanes/integ3-NOTES.md. Leave resolved working tree, NO commit (reviewer commits).
