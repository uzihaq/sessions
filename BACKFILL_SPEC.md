# Codex rollout BACKFILL on watcher attach
Work ONLY in /Users/uzair/pretty-PTY-backfill. Files: prettyd/src/codexWatcher.ts (+ sessions.ts only if the attach callsite needs a flag). Gate: npm run build clean. No commit. NEVER touch the live daemon.
BUG REMAINDER (board tsk_0c96481a): resolver now finds the rollout for reattached codex sessions, but watchCodexRollout only TAILS — it emits events from the current file end. So a reattached idle session renders empty until new activity. FIX: when the watcher attaches to an EXISTING rollout file, first REPLAY its existing content through the same normalize→emit path (codexNormalize), bounded: if the file is huge, replay only the last ~2000 lines (read tail efficiently — read file, split, slice; files are typically <5MB so a full read + slice is fine, but cap memory at ~16MB, skip earlier). Order guarantee: replay fully BEFORE emitting live-tail events (queue any tail events that arrive during replay, or attach tail after replay completes using the recorded offset so nothing is missed or duplicated — track byte offset at replay end and tail from there). Dedup: none needed if offset handoff is exact. Emit through the exact same event callback so claudeEventLog/Pretty view populate identically to live events.
Prove it (BACKFILL_NOTES.md): a test script that points watchCodexRollout at a REAL existing rollout from ~/.codex/sessions (read-only) and asserts N>0 normalized events are emitted immediately (the backfill) and that a line appended after attach is ALSO emitted exactly once (offset handoff, no dup). npm run build clean. No commit.
