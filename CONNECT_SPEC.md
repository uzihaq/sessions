# PR4: the frontend can actually connect to a remote/HTTPS daemon
Work ONLY in /Users/uzair/pretty-PTY-connect/frontend/. Gate: npx tsc --noEmit clean + npx vite build clean. No commit.
Today the web UI cannot be pointed at an HTTPS endpoint: ServerSelector (src/components/ServerSelector.tsx ~:47-139) collects only name/host/port — never scheme or token — so every added server is http/ws (src/lib/servers.ts has scheme+token fields and api/prettyd.ts maps https→wss correctly; the UI just never sets them). Fix:
1. ServerSelector: replace host/port fields with ONE "endpoint" field that accepts a full URL (https://mac.tailnet.ts.net, http://100.x.x.x:8787, localhost:8787 → assume http for localhost/IP, https for names). Parse into {scheme, host, port(default 443/80/8787)}. Keep an "advanced" reveal with an optional token field (persisted into ServerConfig.token, which the api layer already sends). Show scheme in the server list rows.
2. Fragment bootstrap in src/App.tsx (or a small src/lib/hostedBootstrap.ts called at startup): on load, parse #endpoint=<url>&token=<t> from location.hash; if present: validate (https or localhost http only), upsert into the servers store (updateServer/addServer in src/lib/servers.ts), select it, and IMMEDIATELY history.replaceState to scrub the secret from the URL/history. Idempotent.
3. frontend/public/sw.js: CACHE_VERSION is hard-pinned 'pretty-pty-v2' (line ~33, has a TODO) — stale-shell trap. Change to a build-injected version: define __BUILD_HASH__ via vite define (Date-free: use a hash of package.json version + a build counter, or import.meta.env.VITE_BUILD_ID with a vite plugin writing it) — simplest robust approach acceptable. On activate, delete old caches (already there) — just make the version actually change per build.
Write CONNECT_NOTES.md with proof (tsc + build output, and a manual trace of the fragment flow). No commit.
