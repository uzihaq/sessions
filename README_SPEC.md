# README rewrite — user-first for the v0.1 public launch
Work ONLY in /Users/uzair/pretty-PTY-readme. File: README.md (rewrite; move current architecture content to docs/ARCHITECTURE.md). No commit.
Audience: someone who runs Claude Code/Codex and found the repo. Structure: 1) What it is (2 sentences: long-lived agent sessions in your browser, survive everything, we never see your data) + link https://pretty-pty.somewhere.site. 2) Install: npm i -g pretty-pty; pretty install; open http://localhost:8787. Requirements macOS + Node 18+. 3) First session + the CLI in 60s (new/ls/send/wait/last/model/kill, --model/--effort/--fast). 4) Remote access (early access): tailscale + pretty remote enable + QR. 5) Notifications + hooks (~/.config/pretty/hooks.json, PRETTY_* env). 6) The trust contract (never see data / no LLM calls / no phone-home / MIT; localhost no-auth, token for remote). 7) Troubleshooting (pretty doctor, xcode-select, uninstall). 8) Development (worktrees, pretty deploy, link ARCHITECTURE.md). Honest early-access tone, no vaporware claims. Keep it under ~180 lines.
