# Launch posture: localhost needs no token; everything else does
Work ONLY in /Users/uzair/pretty-PTY-auth. Files: prettyd/src/http.ts, ws.ts, config.ts (+test script). Gate: npm run build clean. No commit. Never touch the live daemon or ~/.local/state/pretty-PTY/open.
V1 promise: "Local-first: no auth for localhost. Token auth for anything else." Today auth is all-or-nothing (token everywhere, or the `open` escape-hatch file disables it everywhere).
BUILD: a request is AUTH-EXEMPT when its TCP peer is loopback (req.socket.remoteAddress is 127.0.0.1/::1/::ffff:127.0.0.1) — applies to checkAuth in http.ts AND the WS upgrade in ws.ts. Non-loopback peers still require the bearer/?token= (unless the open file exists — keep that escape hatch exactly as-is for trusted-tailnet mode). SECURITY: exemption is by SOCKET PEER ONLY, never by Host/Origin header (spoofable). Note: a tailscale-serve proxy terminates on loopback and would inherit the exemption — mitigate: if the request carries a Tailscale-Serve/X-Forwarded-For header (tailscale serve sets X-Forwarded-For), do NOT exempt. Document this in a comment. Origin/CSWSH checks stay unchanged for everyone.
PROVE (AUTH_NOTES.md): unit-style test of the exemption predicate (loopback+no-XFF→exempt; loopback+XFF→not; 100.x→not); build clean.
